import { config } from 'dotenv';
config();

// This file is used to register Genkit flows for development.
// No need to import specific flows here anymore if they are being removed.
// If you add new flows, you might import them here for dev purposes.
